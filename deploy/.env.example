# Copy to `.env` (gitignored), then fill in. Ports and image tag are written directly in the
# compose files: edit those instead. See docs/runbooks/deployment.md.

# Required. Passwords you choose; no `change-me` must remain.
AMULE_EC_PASSWORD=change-me
WEBUI_PWD=change-me

# Your own uid and gid (`id -u`, `id -g`), so deploy/data, deploy/amule and deploy/downloads
# stay readable without sudo.
PUID=1000
PGID=1000

# VPN stack (gluetun.compose.yml) only. The key is required, the rest have defaults.
WIREGUARD_PRIVATE_KEY=change-me
VPN_SERVICE_PROVIDER=protonvpn
SERVER_COUNTRIES=
VPN_PORT_FORWARDING=off
