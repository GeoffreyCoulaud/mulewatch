# targets.yml SMOKE — une cible minimale (meme schema que config/targets.yml / parse_targets).
# Suffit a monter le MatchingEngine ; le smoke verifie le cablage, pas la pertinence du match.
episodes:
  - season: 2
    seasonal_number: 11
    absolute_number: 62
    segments:
      - { letter: A, title: "Les demoiselles cambrioleuses" }
