# SMOKE targets.yml: one minimal target (same schema as config/targets.yml / parse_targets).
# Enough to build the MatchingEngine; the smoke checks the wiring, not the match quality.
episodes:
  - season: 2
    seasonal_number: 11
    absolute_number: 62
    segments:
      - { letter: A, title: "Les demoiselles cambrioleuses" }
